# This directory is intentionally kept empty in git.
#
# n8n credentials are stored ENCRYPTED inside Postgres (keyed by N8N_ENCRYPTION_KEY).
# Never export decrypted credentials into this repo. The .gitignore blocks *.json
# here precisely so a stray `n8n export:credentials --decrypted` cannot leak secrets.
